using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace Models
{
    public enum Sections
    {
        OVERVIEW = 0,
        CHARACTERS = 1,
        CAST = 2,
        REVIEWS = 3
    }
}